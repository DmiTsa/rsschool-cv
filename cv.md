![pngtree-male-avatar-simple-cartoon-design-png-image_1934458](https://github.com/user-attachments/assets/22cc31e4-bfd9-44d3-9ddd-534bb0732699)

# Dmitry Taskaev

## telegramm: fire_d(dmi_tsa)


Очередной заход на RS JS
### Skills:
  * Base html, css, js.
  * Base react
  * Base nodeJs

```
 diceSet.state.forEach((el, i) => {
        const node = document.createElement('img')
        node.classList.add('dice')

        if (el === 0) {
            node.src = diceImgPathDefault
        } else {
            node.src = diceImgPaths[el - 1]
            node.id = `dice${i}`
            if (currentDiceSet.blocked[i]) {
                node.classList.add('dice_block')
            }
        }

        destNode.appendChild(node)
    })
```

### learning: High BSUIR
### English level: A0-A1
